'use strict';

const React = require('react');

const SearchForm = React.createClass({

    render() {

        return (
          <h1>Hello, World!</h1>
        );
    }
});

module.exports = SearchForm;
